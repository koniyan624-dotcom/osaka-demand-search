# 引き継ぎ資料 — 大阪需要サーチ（タクシー需要ダッシュボード）

Claude Codeなど別セッションでこのプロジェクトを引き継ぐ人（AI・人間どちらも）向けのメモです。
セットアップ手順そのものは `README.md` を参照してください。ここには「なぜ今の形になったか」「何が決まっていて、何が決まっていないか」を書いています。

## 誰が使うか

- 利用者：小西さん（大阪でタクシー業務。以前は伊丹空港中心の需要予測を検討していたが、空港情報は不要と判断し外した）
- 用途：営業中に見る、空車待機の判断材料
- 想定規模：まずは本人＋友人数名。将来的には複数の同業者に配る可能性あり（人数未定・不特定多数ではない想定）

## 経緯（重要な意思決定）

1. 最初はApp Store のタクシー需要予測アプリ（クラスターサーチ）を参考に、伊丹空港到着便の予測アプリとしてClaude Artifact（db機能）で試作した。
2. 途中で「鉄道情報」「飲食店閉店情報」を追加。さらに「イベント・ライブ・コンサート」の追加を検討し、4項目の優先順位を相談された。
3. こちらの提案で優先順位は **飲食店 → 鉄道 → イベント → 空港** と回答したところ、「空港は要らない」と言われ、最終的に **飲食店・鉄道運行・イベント** の3本立てに確定。対象エリアは大阪市・堺市・東大阪市・八尾市・吹田市・豊中市の6市。
4. Claude Artifact（db機能）で3タブ版を作ったが、**共有した友人がClaudeにログインしないと見られない**問題が発覚。db機能は「サインイン済みの同一組織ユーザーのみ」が前提のため。
5. ログイン不要にすると「各自の端末にしかデータが残らず共有できない」というトレードオフがあることを説明。
6. 小西さんから「複数人での利用の可能性がある」「以前Claude Codeで作った議事録作成アプリの時と同じ方式（GitHub）にしてほしい」との指示。
7. **GitHub Pages（静的ホスティング・無料・ログイン不要）＋ Firebase Firestore（無料・共有DB）** の構成に切り替えることで合意。今回のファイル一式はこの構成で作られている。
8. 今回、「Claude Codeで進めてもらう」ということで、このセッションから引き継ぐことになった。

## 今の構成

```
index.html          … 画面本体（HTML+CSS+JS、ビルド不要のバニラJS）
firebase-config.js   … Firebase接続設定（プレースホルダのまま。要入力）
firestore.rules      … Firestoreセキュリティルール（誰でも読み書き可、簡易設定）
README.md            … Firebase作成〜GitHub Pages公開までの手順書
HANDOFF.md            … このファイル
```

- フレームワーク・ビルドツールは使用していない（npm等不要。index.htmlを開くだけで動く設計思想）。
- Firebase SDKはCDN経由のESモジュールで読み込み（`https://www.gstatic.com/firebasejs/10.12.2/...`）。
- フォントはGoogle Fonts（Zen Kaku Gothic New / Noto Sans JP / JetBrains Mono）をCDNから読み込み。
- ライト/ダーク両テーマ対応（`prefers-color-scheme` で自動切替）。

## データモデル（Firestoreコレクション）

### `venues`（飲食店閉店情報）
| フィールド | 型 | 内容 |
|---|---|---|
| name | string | エリア・店舗名 |
| city | string | 6市のいずれか（未設定可） |
| time | string "HH:MM" | 閉店時刻。24:00以降は "25:00"＝翌1:00 のように表記可 |
| genre | string | 任意。ジャンル表記 |
| days | boolean[7] | 曜日フラグ。インデックス0=日曜〜6=土曜 |
| isSample | boolean | サンプルデータかどうか |
| updatedAt | ISO string | 最終更新時刻 |

### `trains`（鉄道運行情報）
| フィールド | 型 | 内容 |
|---|---|---|
| name | string | 路線名（在来線・新幹線問わず同じコレクション） |
| areas | string[] | 関連する市区（複数可、目安） |
| status | "normal" \| "delay" \| "suspend" | 現在のステータス。手動更新 |
| updatedAt | ISO string | 最終更新時刻 |

初期登録は18路線（JR各線・阪急・阪神・近鉄・南海・大阪メトロ主要線・大阪モノレール・新幹線）。区間と市区の対応はあくまで目安。

### `events`（イベント・ライブ・コンサート）
| フィールド | 型 | 内容 |
|---|---|---|
| venue | string | 会場名（候補: 大阪城ホール、京セラドーム大阪、フェスティバルホール、グランキューブ大阪、なんばHatch、Zepp Osaka Bayside、オリックス劇場、パナソニック スタジアム吹田、東大阪市花園ラグビー場、堺市産業振興センター。自由入力も可） |
| title | string | 公演名（任意） |
| date | string "YYYY-MM-DD" | 開催日 |
| endTime | string "HH:MM" | 終演予定時刻 |
| city | string | 6市のいずれか |
| genre | string | 任意（ライブ／スポーツ／展示会など） |
| isSample | boolean | サンプルデータかどうか |

各コレクションとも、初回アクセス時にコレクションが空ならサンプルデータを自動投入する仕組み（`maybeSeed()`）。本番データを入れ始めたらサンプルは各タブの「サンプルを削除する」ボタンで一括削除可能。

## UI/表示ロジックのポイント

- 「飲食店」「イベント」は残り時間で色分け（赤＝差し迫っている／オレンジ＝そろそろ／緑＝まだ余裕）。閾値は飲食店が15分・45分、イベントが20分・60分（コード内 `venueStatusClass` / `eventStatusClass` 参照）。
- 「鉄道」は平常／遅延／見合わせの3段階を手動ボタンで切替。見合わせ・遅延の路線が上に自動でソートされる。
- イベントは日付を跨いだ表示に対応（今日／今日・明日／7日以内／全件のスコープ切替）。
- 時刻は "25:30"（＝翌1:30）のような24時超え表記に対応した独自パーサーを使用（`parseTime` 関数）。標準の `<input type="time">` は24時超えを扱えないため、あえて `type="text"` にしている。

## 未確定・要フォローアップ

- [ ] `firebase-config.js` は未入力（プレースホルダのまま）。実際のFirebaseプロジェクトの値を入れる必要あり
- [ ] GitHubリポジトリは未作成（このセッションでは作成できないため、小西さん側で新規作成が必要）
- [ ] サンプルデータを実データに置き換える作業は未着手
- [ ] Firestoreのセキュリティルールは「誰でも読み書き可」の簡易設定。荒らし対策が必要になった場合は認証の追加を検討
- [ ] 複数人での本格利用が決まった場合、利用人数や運用ルール（誰が編集してよいか等）は未整理
- [ ] 会場・路線のエリア割り当ては目安ベースで、精査はしていない
